# Neural traces experiment configuration
defaults:
  - hmog_diff
  - _self_

latent_dim: 100
n_clusters: 50

num_cycles: 20

lgm:
  n_epochs: 0

mix:
  n_epochs: 0

full:
  n_epochs: 200
  batch_steps: 2000
  lr_init: 3e-4
  grad_clip: 10
  min_prob: 1e-4
  obs_min_var: 1e-5
  lat_min_var: 1e-5
  eigen_floor_prs: 1e-5
  l1_reg: 0
  l2_reg: 0
